import Route from '@ember/routing/route';

/**
 * For simplicity's sake, assume that a user only have access to one single
 * business and one single branch.
 * Reaching this route implies that such a business and branch are defined.
 */
export default class ClientRoute extends Route {}
